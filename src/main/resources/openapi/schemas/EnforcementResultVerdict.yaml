type: object
description: The verdict returned against an offence.
x-class-extra-annotation: "@lombok.AllArgsConstructor"
properties:
  verdictDate:
    description: Date on which the verdict was returned
    $ref: './DatePattern.yaml'
  verdictCategory:
    description: The verdict category, e.g. CONVICTED, ACQUITTED.
    type: string
  verdictDescription:
    description: Human-readable description of the verdict
    type: string
required:
  - verdictDate
  - verdictCategory
additionalProperties: false
